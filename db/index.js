const { getAllTimers, getCurrentTimer, getLastTimer } = require('./timers');
const getAllUsers = require('./users');

module.exports = {
  getAllTimers,
  getAllUsers,
  getCurrentTimer,
  getLastTimer,
};
